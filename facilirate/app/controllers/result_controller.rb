class ResultController < ApplicationController
    # getResults
    # URL params: type, building, room # (all optional) => Obtain from Model
    # Return in view: Results table with each cell containing link to specific facility, average ratings and reviews
    #
end
